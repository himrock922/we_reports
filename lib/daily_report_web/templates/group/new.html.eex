<h1>新規グループ作成</h1>

<%= render "form.html", Map.put(assigns, :action, Routes.group_path(@conn, :create)) %>

<span><%= link "グループ一覧に戻る", class: "button", to: Routes.group_path(@conn, :index) %></span>
