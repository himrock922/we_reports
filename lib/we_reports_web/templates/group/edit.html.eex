<h1>グループ編集</h1>

<%= render "form.html", Map.put(assigns, :action, Routes.group_path(@conn, :update, @group)) %>

<span><%= link "グループ一覧に戻る", to: Routes.group_path(@conn, :index), class: "btn btn-outline-secondary"%></span>
