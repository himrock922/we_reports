<div class="container">
  <h1><%= "#{@proposition.group.name} #{@proposition.name}" %>の編集</h1>
  <%= render "form.html", Map.put(assigns, :action, Routes.group_proposition_path(@conn, :update, @proposition.group_id, @proposition)) %>
  <span><%= link "案件一覧に戻る", class: "btn btn-outline-secondary", to: Routes.group_proposition_path(@conn, :index, @proposition.group_id) %></span>
</div>
